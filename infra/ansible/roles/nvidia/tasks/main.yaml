---
# NVIDIA role - Driver, Container Toolkit, and K8s Device Plugin
#
# Supports Secure Boot with two-step MOK (Machine Owner Key) enrollment:
#
# Step 1 (remote): Run this role - installs driver, queues MOK for enrollment
# Step 2 (physical): Reboot and approve MOK at the blue UEFI screen
# Step 3 (remote): Re-run this role - driver loads, completes setup
#
# Control via environment variable:
#   NVIDIA_ENABLED=true ansible-playbook playbooks/site.yaml

# =============================================================================
# Prerequisites
# =============================================================================

- name: Skip if NVIDIA disabled
  ansible.builtin.debug:
    msg: "NVIDIA GPU support disabled via nvidia_enabled={{ nvidia_enabled }}"
  when: not nvidia_enabled

- name: End play if NVIDIA disabled
  ansible.builtin.meta: end_host
  when: not nvidia_enabled

- name: Check for NVIDIA GPU
  ansible.builtin.shell: lspci | grep -i nvidia
  register: nvidia_gpu_check
  failed_when: false
  changed_when: false

- name: Skip if no NVIDIA GPU found
  ansible.builtin.debug:
    msg: "No NVIDIA GPU found, skipping NVIDIA setup"
  when: nvidia_gpu_check.rc != 0

- name: End play if no GPU
  ansible.builtin.meta: end_host
  when: nvidia_gpu_check.rc != 0

- name: Display detected GPU
  ansible.builtin.debug:
    msg: "Detected NVIDIA GPU: {{ nvidia_gpu_check.stdout }}"

# =============================================================================
# Secure Boot Detection
# =============================================================================

- name: Check Secure Boot status
  ansible.builtin.command: mokutil --sb-state
  register: secureboot_state
  failed_when: false
  changed_when: false

- name: Set secure_boot_enabled fact
  ansible.builtin.set_fact:
    secure_boot_enabled: "{{ 'SecureBoot enabled' in secureboot_state.stdout }}"

- name: Display Secure Boot status
  ansible.builtin.debug:
    msg: "Secure Boot: {{ 'ENABLED' if secure_boot_enabled else 'DISABLED' }}"

# =============================================================================
# Nouveau conflict handling
# =============================================================================

- name: Check if nouveau is loaded
  ansible.builtin.shell: lsmod | grep -q nouveau
  register: nouveau_loaded
  failed_when: false
  changed_when: false

- name: Ensure nouveau blacklist is active
  ansible.builtin.copy:
    content: |
      # Managed by Ansible - blacklist nouveau to prevent it from claiming the GPU
      blacklist nouveau
      options nvidia-drm modeset=1
      options nvidia NVreg_TemporaryFilePath=/var/tmp
      options nvidia NVreg_EnableS0ixPowerManagement=1
      options nvidia NVreg_PreserveVideoMemoryAllocations=1
    dest: /etc/modprobe.d/nvidia.conf
    mode: "0644"
  register: nvidia_modprobe_conf

- name: Remove stale .dpkg-new blacklist file
  ansible.builtin.file:
    path: /etc/modprobe.d/nvidia.conf.dpkg-new
    state: absent

- name: Rebuild initramfs if blacklist changed
  ansible.builtin.command: update-initramfs -u
  changed_when: true
  when: nvidia_modprobe_conf is changed

- name: Fail if nouveau is loaded (reboot required)
  ansible.builtin.fail:
    msg: >
      nouveau is loaded and holding the GPU. The blacklist has been configured
      and initramfs rebuilt. Please reboot the server and re-run this playbook.
  when: nouveau_loaded.rc == 0

# =============================================================================
# Check if driver is already working
# =============================================================================

- name: Check if nvidia kernel module is loaded and working
  ansible.builtin.shell: |
    timeout 10 modprobe nvidia 2>/dev/null || true
    cat /proc/driver/nvidia/version 2>/dev/null | grep -oP 'NVRM version: NVIDIA UNIX.*\K[0-9]+\.[0-9]+\.[0-9]+' | head -1
  register: nvidia_module_version
  failed_when: false
  changed_when: false

- name: Check if nvidia-smi binary exists
  ansible.builtin.stat:
    path: /usr/bin/nvidia-smi
  register: nvidia_smi_binary

- name: Check nvidia-smi version if it exists
  ansible.builtin.shell: nvidia-smi --version 2>/dev/null | grep -oP 'NVIDIA-SMI \K[0-9]+\.[0-9]+\.[0-9]+'
  register: nvidia_smi_version
  when: nvidia_smi_binary.stat.exists
  failed_when: false
  changed_when: false

- name: Set driver facts
  ansible.builtin.set_fact:
    nvidia_module_working: "{{ nvidia_module_version.rc == 0 and nvidia_module_version.stdout | length > 0 }}"
    nvidia_smi_working: "{{ nvidia_smi_binary.stat.exists and nvidia_smi_version.rc == 0 }}"
    driver_version_current: "{{ nvidia_module_version.stdout | default('none') | trim }}"
    smi_version_current: "{{ nvidia_smi_version.stdout | default('none') | trim }}"
    driver_working: "{{ nvidia_module_version.rc == 0 and nvidia_module_version.stdout | length > 0 }}"

- name: Display driver status
  ansible.builtin.debug:
    msg: |
      Kernel module version: {{ driver_version_current }}
      nvidia-smi version: {{ smi_version_current }}
      Module working: {{ nvidia_module_working }}
      nvidia-smi working: {{ nvidia_smi_working }}

# =============================================================================
# Install nvidia-smi if missing (Debian package issue workaround)
# =============================================================================

- name: Download NVIDIA driver for nvidia-smi extraction
  ansible.builtin.get_url:
    url: "{{ nvidia_driver_url }}"
    dest: /tmp/NVIDIA-driver.run
    mode: "0755"
  when: nvidia_module_working and not nvidia_smi_working

- name: Extract nvidia-smi from installer
  ansible.builtin.shell: |
    cd /tmp
    ./NVIDIA-driver.run --extract-only
    cp NVIDIA-Linux-x86_64-{{ nvidia_driver_version_clean }}/nvidia-smi /usr/bin/
    chmod +x /usr/bin/nvidia-smi
    rm -rf NVIDIA-Linux-x86_64-{{ nvidia_driver_version_clean }}
  when: nvidia_module_working and not nvidia_smi_working

- name: Clean up driver installer after extraction
  ansible.builtin.file:
    path: /tmp/NVIDIA-driver.run
    state: absent
  when: nvidia_module_working and not nvidia_smi_working

# =============================================================================
# Full driver installation (only if module not working)
# =============================================================================

# =============================================================================
# NVIDIA CUDA Repository Setup
# =============================================================================

- name: Install prerequisites
  ansible.builtin.apt:
    name:
      - build-essential
      - linux-headers-{{ ansible_facts['kernel'] }}
      - pkg-config
      - libglvnd-dev
      - dkms
      - mokutil
    state: present
    update_cache: yes
  when: not nvidia_module_working

- name: Download NVIDIA CUDA keyring
  ansible.builtin.get_url:
    url: "{{ nvidia_cuda_keyring_url }}"
    dest: /tmp/cuda-keyring.deb
    mode: "0644"
  when: not nvidia_module_working

- name: Install NVIDIA CUDA keyring
  ansible.builtin.apt:
    deb: /tmp/cuda-keyring.deb
    state: present
  when: not nvidia_module_working

- name: Update apt cache after adding CUDA repo
  ansible.builtin.apt:
    update_cache: yes
  when: not driver_working

# =============================================================================
# NVIDIA Driver Installation (via DKMS)
# =============================================================================

- name: Install NVIDIA driver packages
  ansible.builtin.apt:
    name:
      - "nvidia-driver={{ nvidia_driver_version }}"
      - "nvidia-kernel-dkms={{ nvidia_driver_version }}"
    state: present
  when: not driver_working
  register: driver_installed

- name: Rebuild initramfs after driver install
  ansible.builtin.command: update-initramfs -u
  changed_when: true
  when: driver_installed is changed

# =============================================================================
# Secure Boot: MOK Enrollment Handling
# =============================================================================

- name: Try loading nvidia module
  community.general.modprobe:
    name: nvidia
    state: present
  register: nvidia_modprobe
  failed_when: false
  when: driver_installed is changed or not nvidia_module_working

- name: Check if module load failed due to Secure Boot
  ansible.builtin.set_fact:
    mok_enrollment_needed: "{{ nvidia_modprobe.rc | default(0) != 0 and secure_boot_enabled }}"

- name: Check if MOK key exists
  ansible.builtin.stat:
    path: /var/lib/dkms/mok.pub
  register: mok_pub_file
  when: mok_enrollment_needed

- name: Check if MOK is already queued for enrollment
  ansible.builtin.command: mokutil --list-new
  register: mok_pending
  failed_when: false
  changed_when: false
  when: mok_enrollment_needed and mok_pub_file.stat.exists | default(false)

- name: Fail if MOK_PASSWORD not set
  ansible.builtin.fail:
    msg: |
      MOK_PASSWORD environment variable is required for Secure Boot systems.
      Add to .env: MOK_PASSWORD=your-password
      This password will be entered at the blue MOK screen during reboot.
  when:
    - mok_enrollment_needed
    - mok_pub_file.stat.exists | default(false)
    - mok_pending.stdout | default('') | trim == ''
    - mok_enrollment_password == ''

- name: Queue MOK for enrollment (requires reboot + physical approval)
  ansible.builtin.shell: |
    printf '%s\n%s\n' "{{ mok_enrollment_password }}" "{{ mok_enrollment_password }}" | mokutil --import /var/lib/dkms/mok.pub
  when:
    - mok_enrollment_needed
    - mok_pub_file.stat.exists | default(false)
    - mok_pending.stdout | default('') | trim == ''
    - mok_enrollment_password != ''
  register: mok_queued

- name: Display MOK enrollment instructions
  ansible.builtin.pause:
    seconds: 1
    prompt: |

      ============================================================================
                        SECURE BOOT: MOK ENROLLMENT REQUIRED
      ============================================================================

      The NVIDIA driver is installed but cannot load due to Secure Boot.
      A Machine Owner Key (MOK) must be enrolled in your UEFI firmware.

      STEP 1: Reboot the machine

          ssh root@{{ ansible_host }} reboot

      STEP 2: At the blue "MOK Management" screen (requires physical access):

          1. Select "Enroll MOK"
          2. Select "Continue"
          3. Enter password: {{ mok_enrollment_password | default('[your MOK_PASSWORD from .env]', true) }}
          4. Select "Reboot"

      STEP 3: Re-run this playbook to complete GPU setup

          ansible-playbook playbooks/site.yaml --tags nvidia

      ============================================================================
  when: mok_enrollment_needed

- name: End play if MOK enrollment needed (will continue after reboot)
  ansible.builtin.meta: end_host
  when: mok_enrollment_needed

# =============================================================================
# Driver Verification (only reached if driver loads successfully)
# =============================================================================

- name: Verify nvidia module is loaded
  community.general.modprobe:
    name: "{{ item }}"
    state: present
  loop:
    - nvidia
    - nvidia_uvm
    - nvidia_modeset
  when: not nvidia_module_working

- name: Verify driver with nvidia-smi
  ansible.builtin.command: nvidia-smi
  register: nvidia_smi_verify
  changed_when: false

- name: Display GPU info
  ansible.builtin.debug:
    msg: "{{ nvidia_smi_verify.stdout }}"

# =============================================================================
# GPU Monitoring Tools
# =============================================================================

- name: Install nvtop for GPU monitoring
  ansible.builtin.apt:
    name: nvtop
    state: present

# =============================================================================
# NVIDIA Container Toolkit
# =============================================================================

- name: Add NVIDIA Container Toolkit GPG key
  ansible.builtin.shell: |
    curl -fsSL https://nvidia.github.io/libnvidia-container/gpgkey | \
      gpg --dearmor -o /usr/share/keyrings/nvidia-container-toolkit-keyring.gpg
  args:
    creates: /usr/share/keyrings/nvidia-container-toolkit-keyring.gpg

- name: Add NVIDIA Container Toolkit repository
  ansible.builtin.shell: |
    curl -s -L https://nvidia.github.io/libnvidia-container/stable/deb/nvidia-container-toolkit.list | \
      sed 's#deb https://#deb [signed-by=/usr/share/keyrings/nvidia-container-toolkit-keyring.gpg] https://#g' | \
      tee /etc/apt/sources.list.d/nvidia-container-toolkit.list
  args:
    creates: /etc/apt/sources.list.d/nvidia-container-toolkit.list

- name: Install NVIDIA Container Toolkit
  ansible.builtin.apt:
    name: nvidia-container-toolkit
    state: present
    update_cache: yes

# =============================================================================
# Configure NVIDIA Container Runtime and K3s containerd
# =============================================================================

# Use CDI mode for nvidia-container-runtime. The default "auto" mode and
# "legacy" mode both invoke nvidia-container-cli's prestart hook which fails
# with "nvml error: unknown error" on host-installed drivers (590.48.01+).
# CDI mode uses a pre-generated device spec file instead, which is more
# reliable and the direction NVIDIA is heading.
- name: Set nvidia-container-runtime to CDI mode
  ansible.builtin.lineinfile:
    path: /etc/nvidia-container-runtime/config.toml
    regexp: '^mode\s*='
    line: 'mode = "cdi"'

# Generate CDI spec so the runtime knows how to inject GPU devices.
# Must be regenerated if the driver version changes.
- name: Create CDI spec directory
  ansible.builtin.file:
    path: /etc/cdi
    state: directory
    mode: "0755"

- name: Generate CDI device spec
  ansible.builtin.command: nvidia-ctk cdi generate --output=/etc/cdi/nvidia.yaml
  changed_when: true

- name: Symlink K3s runc to system PATH for nvidia-container-runtime
  ansible.builtin.file:
    src: /var/lib/rancher/k3s/data/current/bin/runc
    dest: /usr/local/bin/runc
    state: link
    force: yes

# NOTE: GPU Operator's toolkit DaemonSet will handle containerd configuration
# We keep our template for reference but GPU Operator will manage the runtime config

# =============================================================================
# NVIDIA RuntimeClass (maps to containerd nvidia handler)
# =============================================================================

- name: Create nvidia RuntimeClass
  kubernetes.core.k8s:
    state: present
    kubeconfig: "{{ k3s_kubeconfig }}"
    definition:
      apiVersion: node.k8s.io/v1
      kind: RuntimeClass
      metadata:
        name: nvidia
      handler: nvidia

# =============================================================================
# NVIDIA GPU Operator
# =============================================================================

- name: Add NVIDIA Helm repository
  kubernetes.core.helm_repository:
    name: nvidia
    repo_url: https://helm.ngc.nvidia.com/nvidia

- name: Deploy NVIDIA GPU Operator
  kubernetes.core.helm:
    name: gpu-operator
    chart_ref: nvidia/gpu-operator
    release_namespace: gpu-operator
    create_namespace: yes
    kubeconfig: "{{ k3s_kubeconfig }}"
    values:
      # Use pre-installed driver on host
      driver:
        enabled: false

      # Disable toolkit containerd configuration - we handle this in k3s role
      # The toolkit's import-based config doesn't work with k3s embedded containerd
      # See: infra/ansible/roles/k3s/templates/containerd-config.toml.tmpl.j2
      toolkit:
        enabled: false

      # Disable CDI - requires the toolkit DaemonSet to configure CDI on the node.
      # Since toolkit is disabled (we configure containerd in k3s role directly),
      # CDI is not available. Without this, the operator injects CDI env vars
      # (DEVICE_LIST_STRATEGY=cdi-annotations,cdi-cri) that override our ConfigMap
      # and cause the device plugin to crash.
      cdi:
        enabled: false

      # Operator configuration
      operator:
        # Use nvidia runtime so device-plugin/GFD daemonsets get NVML access.
        # The nvidia-container-runtime injects /dev/nvidia* and libnvidia-ml.so
        # into containers. The operator controller pod itself doesn't need GPU
        # access but nvidia runtime falls back gracefully for non-GPU workloads.
        runtimeClass: "nvidia"

      # Validator disabled - designed for GPU Operator managed drivers/toolkit
      # We verify GPU via kubectl command instead (see end of role)
      validator:
        enabled: false

      # Enable device plugin
      # Use NVML discovery (not "auto" which fails with host-installed drivers)
      # CDI device-list-strategy requires the toolkit, so use legacy envvar listing
      devicePlugin:
        enabled: true
        config:
          create: true
          name: "device-plugin-config"
          default: "host-driver"
          data:
            host-driver: |-
              version: v1
              flags:
                migStrategy: none
                deviceDiscoveryStrategy: nvml
                deviceListStrategy: envvar

      # Disable components we don't need
      migManager:
        enabled: false
      gfd:
        enabled: true
      dcgm:
        enabled: false
      dcgmExporter:
        enabled: false

      # Node feature discovery (required for GPU node labeling)
      nfd:
        enabled: true

# Toolkit is disabled - we configure containerd in k3s role directly.
# Create validation files for GPU Operator components that expect them.
# /run is a tmpfs, so we use systemd-tmpfiles to recreate them on every boot.

- name: Install tmpfiles.d rule for nvidia validation files
  ansible.builtin.copy:
    content: |
      # Create GPU Operator validation files on boot (toolkit/driver disabled, host-installed)
      d /run/nvidia/validations 0755 root root -
      f /run/nvidia/validations/.driver-ctr-ready 0644 root root - DRIVER_READY=true
      f /run/nvidia/validations/driver-ready 0644 root root - DRIVER_READY=true
      f /run/nvidia/validations/toolkit-ready 0644 root root - TOOLKIT_READY=true
    dest: /etc/tmpfiles.d/nvidia-validations.conf
    mode: "0644"
  register: nvidia_tmpfiles

- name: Apply tmpfiles.d rule now
  ansible.builtin.command: systemd-tmpfiles --create /etc/tmpfiles.d/nvidia-validations.conf
  changed_when: true
  when: nvidia_tmpfiles.changed

# Disable validator via node label - it fails with pre-installed drivers
# and we already verify GPU via kubectl command
- name: Label node to disable GPU Operator validator
  kubernetes.core.k8s:
    state: present
    kubeconfig: "{{ k3s_kubeconfig }}"
    definition:
      apiVersion: v1
      kind: Node
      metadata:
        name: "{{ ansible_hostname }}"
        labels:
          nvidia.com/gpu.deploy.operator-validator: "false"

- name: Wait for GPU Operator device plugin to be ready
  kubernetes.core.k8s_info:
    api_version: apps/v1
    kind: DaemonSet
    name: nvidia-device-plugin-daemonset
    namespace: gpu-operator
    kubeconfig: "{{ k3s_kubeconfig }}"
  register: device_plugin_status
  until: device_plugin_status.resources[0].status.numberReady | default(0) > 0
  retries: 30
  delay: 1

- name: Verify GPU is visible to Kubernetes
  ansible.builtin.shell: |
    kubectl get nodes -o json | jq -r '.items[].status.allocatable["nvidia.com/gpu"] // "0"'
  environment:
    KUBECONFIG: "{{ k3s_kubeconfig }}"
  register: k8s_gpu_check
  changed_when: false
  failed_when: k8s_gpu_check.stdout | int < 1

- name: Display K8s GPU status
  ansible.builtin.debug:
    msg: |
      ╔══════════════════════════════════════════════════════════════════════════╗
      ║                         GPU SETUP COMPLETE                                ║
      ╠══════════════════════════════════════════════════════════════════════════╣
      ║  Kubernetes sees {{ k8s_gpu_check.stdout | trim }} NVIDIA GPU(s)                                            ║
      ║                                                                          ║
      ║  Monitoring commands:                                                    ║
      ║    nvidia-smi              - GPU status                                  ║
      ║    nvtop                   - Interactive GPU monitor                     ║
      ║                                                                          ║
      ║  Ollama deployment:                                                      ║
      ║    Set OLLAMA_ENABLED=true and re-run playbook                           ║
      ╚══════════════════════════════════════════════════════════════════════════╝
