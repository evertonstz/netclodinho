# Redis for JuiceFS metadata (separate from session storage)
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-juicefs-data
  namespace: netclode
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path  # k3s default storage class
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis-juicefs
  namespace: netclode
  labels:
    app: redis-juicefs
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis-juicefs
  template:
    metadata:
      labels:
        app: redis-juicefs
    spec:
      priorityClassName: netclode-infrastructure
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
          resources:
            requests:
              memory: "512Mi"
              cpu: "100m"
            limits:
              memory: "1Gi"
              cpu: "500m"
          args:
            - redis-server
            - --appendonly
            - "yes"
            - --appendfsync
            - everysec
            - --maxmemory
            - 768mb
            - --maxmemory-policy
            - noeviction
          volumeMounts:
            - name: data
              mountPath: /data
          livenessProbe:
            exec:
              command: [redis-cli, ping]
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            exec:
              command: [redis-cli, ping]
            initialDelaySeconds: 5
            periodSeconds: 5
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: redis-juicefs-data
---
apiVersion: v1
kind: Service
metadata:
  name: redis-juicefs
  namespace: netclode
  labels:
    app: redis-juicefs
spec:
  selector:
    app: redis-juicefs
  ports:
    - port: 6379
      targetPort: 6379
