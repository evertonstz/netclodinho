// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: netclode/v1/client.proto

package netclodev1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/angristan/netclode/services/control-plane/gen/netclode/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// ClientServiceName is the fully-qualified name of the ClientService service.
	ClientServiceName = "netclode.v1.ClientService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// ClientServiceConnectProcedure is the fully-qualified name of the ClientService's Connect RPC.
	ClientServiceConnectProcedure = "/netclode.v1.ClientService/Connect"
)

// ClientServiceClient is a client for the netclode.v1.ClientService service.
type ClientServiceClient interface {
	// Connect establishes a bidirectional stream for all client-server communication.
	Connect(context.Context) *connect.BidiStreamForClient[v1.ClientMessage, v1.ServerMessage]
}

// NewClientServiceClient constructs a client for the netclode.v1.ClientService service. By default,
// it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and
// sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC()
// or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewClientServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) ClientServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	clientServiceMethods := v1.File_netclode_v1_client_proto.Services().ByName("ClientService").Methods()
	return &clientServiceClient{
		connect: connect.NewClient[v1.ClientMessage, v1.ServerMessage](
			httpClient,
			baseURL+ClientServiceConnectProcedure,
			connect.WithSchema(clientServiceMethods.ByName("Connect")),
			connect.WithClientOptions(opts...),
		),
	}
}

// clientServiceClient implements ClientServiceClient.
type clientServiceClient struct {
	connect *connect.Client[v1.ClientMessage, v1.ServerMessage]
}

// Connect calls netclode.v1.ClientService.Connect.
func (c *clientServiceClient) Connect(ctx context.Context) *connect.BidiStreamForClient[v1.ClientMessage, v1.ServerMessage] {
	return c.connect.CallBidiStream(ctx)
}

// ClientServiceHandler is an implementation of the netclode.v1.ClientService service.
type ClientServiceHandler interface {
	// Connect establishes a bidirectional stream for all client-server communication.
	Connect(context.Context, *connect.BidiStream[v1.ClientMessage, v1.ServerMessage]) error
}

// NewClientServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewClientServiceHandler(svc ClientServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	clientServiceMethods := v1.File_netclode_v1_client_proto.Services().ByName("ClientService").Methods()
	clientServiceConnectHandler := connect.NewBidiStreamHandler(
		ClientServiceConnectProcedure,
		svc.Connect,
		connect.WithSchema(clientServiceMethods.ByName("Connect")),
		connect.WithHandlerOptions(opts...),
	)
	return "/netclode.v1.ClientService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case ClientServiceConnectProcedure:
			clientServiceConnectHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedClientServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedClientServiceHandler struct{}

func (UnimplementedClientServiceHandler) Connect(context.Context, *connect.BidiStream[v1.ClientMessage, v1.ServerMessage]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("netclode.v1.ClientService.Connect is not implemented"))
}
