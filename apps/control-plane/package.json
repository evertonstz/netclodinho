{
  "name": "@netclode/control-plane",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build ./src/index.ts --outdir ./dist --target bun",
    "start": "bun run dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@kubernetes/client-node": "^1.0.0",
    "@netclode/protocol": "workspace:*",
    "ioredis": "^5.4.1"
  },
  "devDependencies": {
    "@types/ioredis": "^5.0.0",
    "typescript": "^5.7.2"
  }
}
